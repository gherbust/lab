{
    // Use IntelliSense para saber los atributos posibles.
    // Mantenga el puntero para ver las descripciones de los existentes atributos.
    // Para más información, visite: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "./app/main.go",
            "env": {
                "connectionString" : "root:bebes2023*@tcp(localhost:3306)/directorio" //no conviene que es te la contraseña en un variable de ambiente, sino en un lugar llamdo "secretos"
            }
        }

    ]
}
